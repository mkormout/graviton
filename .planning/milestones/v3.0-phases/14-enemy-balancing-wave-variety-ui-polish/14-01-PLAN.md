---
phase: 14-enemy-balancing-wave-variety-ui-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prefabs/enemies/beeliner/beeliner.tscn
  - prefabs/enemies/sniper/sniper.tscn
  - prefabs/enemies/flanker/flanker.tscn
  - prefabs/enemies/swarmer/swarmer.tscn
  - prefabs/enemies/suicider/suicider.tscn
  - prefabs/enemies/suicider/suicider-explosion.tscn
autonomous: true
requirements: [ENM-16, ENM-17, ENM-18, ENM-19, ENM-20, ENM-21, ENM-22]

must_haves:
  truths:
    - "All enemies have doubled HP compared to their pre-phase values"
    - "All firing enemies have doubled fight_range compared to their pre-phase values"
    - "All firing enemies have bullet_speed increased by 1.4x"
    - "Sniper, Flanker, and Swarmer Polygon2D shapes point a vertex toward +X (facing direction)"
    - "Suicider has +30% max_speed and thrust"
    - "Suicider explosion has +50% radius and proportionally increased damage"
  artifacts:
    - path: "prefabs/enemies/beeliner/beeliner.tscn"
      provides: "Beeliner stat overrides"
      contains: "max_health = 60"
    - path: "prefabs/enemies/sniper/sniper.tscn"
      provides: "Sniper stat overrides"
      contains: "max_health = 100"
    - path: "prefabs/enemies/flanker/flanker.tscn"
      provides: "Flanker stat overrides"
      contains: "max_health = 80"
    - path: "prefabs/enemies/swarmer/swarmer.tscn"
      provides: "Swarmer stat overrides"
      contains: "max_health = 30"
    - path: "prefabs/enemies/suicider/suicider.tscn"
      provides: "Suicider stat overrides"
      contains: "max_health = 40"
    - path: "prefabs/enemies/suicider/suicider-explosion.tscn"
      provides: "Buffed explosion"
      contains: "radius = 1013.0"
  key_links: []
---

<objective>
Buff all enemy stat exports in .tscn scene files and rotate Polygon2D shapes to align vertices with the facing direction.

Purpose: Per D-09, D-10, D-11, D-12 — enemies must take noticeably more hits (HP x2), engage at longer range (fight_range x2), fire faster projectiles (bullet_speed x1.4), and visually orient a vertex toward the player. Suicider gets speed and explosion buffs per D-17.

Output: 6 updated .tscn files with new export values and Polygon2D rotation offsets.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-enemy-balancing-wave-variety-ui-polish/14-CONTEXT.md
@.planning/phases/14-enemy-balancing-wave-variety-ui-polish/14-RESEARCH.md
@.planning/phases/14-enemy-balancing-wave-variety-ui-polish/14-PATTERNS.md

<interfaces>
<!-- Enemy scene export values are set as plain key=value lines inside the root [node] block of each .tscn file. -->
<!-- Example from beeliner.tscn root node block: -->
<!--
[node name="Beeliner" type="RigidBody2D" ...]
script = ExtResource("1_beeliner")
max_health = 30
detection_radius = 10000.0
max_speed = 2000.0
thrust = 1500.0
score_value = 100
fight_range = 8000.0
-->

<!-- Polygon2D rotation is set on the [node name="Shape" type="Polygon2D" ...] child node: -->
<!-- Add `rotation = X.XXXXX` line (radians) to the Shape node block. -->

<!-- Suicider explosion values are in suicider-explosion.tscn: -->
<!-- Root node: radius = 675.0, power = 15000 -->
<!-- Sub-resource Resource_dmg1: energy = 17500.0, kinetic = 5000.0 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update enemy stat exports (HP x2, fight_range x2, bullet_speed x1.4, suicider speed/explosion buffs)</name>
  <files>
    prefabs/enemies/beeliner/beeliner.tscn,
    prefabs/enemies/sniper/sniper.tscn,
    prefabs/enemies/flanker/flanker.tscn,
    prefabs/enemies/swarmer/swarmer.tscn,
    prefabs/enemies/suicider/suicider.tscn,
    prefabs/enemies/suicider/suicider-explosion.tscn
  </files>
  <read_first>
    prefabs/enemies/beeliner/beeliner.tscn,
    prefabs/enemies/sniper/sniper.tscn,
    prefabs/enemies/flanker/flanker.tscn,
    prefabs/enemies/swarmer/swarmer.tscn,
    prefabs/enemies/suicider/suicider.tscn,
    prefabs/enemies/suicider/suicider-explosion.tscn,
    components/sniper.gd
  </read_first>
  <action>
Update the root `[node]` block export values in each enemy .tscn file. These are plain text key=value lines inside the `[node name="EnemyName" type="RigidBody2D" ...]` block.

**beeliner.tscn** — change these values in the root node block (per D-09, D-10, D-11):
- `max_health = 60` (was 30)
- `fight_range = 16000.0` (was 8000, scene override — doubled)
- `bullet_speed = 6160.0` (was 4400 — x1.4)

**sniper.tscn** — change/add these values in the root node block:
- `max_health = 100` (was 50)
- `fight_range = 22000.0` (was 11000 script default — doubled)
- `comfort_range = 20000.0` (was 10000 script default — doubled)
- `flee_range = 8000.0` (was 4000 script default — doubled)
- `safe_range = 14000.0` (was 7000 script default — doubled)
- `bullet_speed = 14000.0` (was 10000 — x1.4)

NOTE: Sniper script defines `fight_range`, `comfort_range`, `flee_range`, `safe_range` as `@export` vars with script defaults. The .tscn scene override takes precedence. If these keys do not yet exist in the .tscn root node block, ADD them. Place them after `score_value` line.

**flanker.tscn** — change/add these values in the root node block:
- `max_health = 80` (was 40)
- `fight_range = 9000.0` (was 4500 script default — doubled)
- `bullet_speed = 8470.0` (was 6050 — x1.4)

If `fight_range` and `bullet_speed` do not yet exist as scene overrides, ADD them to the root node block.

**swarmer.tscn** — change/add these values in the root node block:
- `max_health = 30` (was 15)
- `fight_range = 10000.0` (was 5000 script default — doubled)
- `bullet_speed = 4900.0` (was 3500 — x1.4)

If `fight_range` and `bullet_speed` do not yet exist as scene overrides, ADD them.

**suicider.tscn** — change these values in the root node block (per D-09, D-17):
- `max_health = 40` (was 20)
- `max_speed = 5200.0` (was 4000 — +30%)
- `thrust = 2600.0` (was 2000 — +30%)

**suicider-explosion.tscn** — change these values (per D-17):
- In the root `[node]` block: `radius = 1013.0` (was 675.0 — +50%)
- In `[sub_resource type="Resource" id="Resource_dmg1"]`: `energy = 26250.0` (was 17500.0 — +50%), `kinetic = 7500.0` (was 5000.0 — +50%)

Score values are already correct (verified: Beeliner 100, Sniper 200, Flanker 150, Swarmer 50, Suicider 75) per D-08. Do NOT change score_value lines.
  </action>
  <verify>
    <automated>cd /Users/milan.kormout/Projects/personal/graviton && grep -n "max_health" prefabs/enemies/beeliner/beeliner.tscn prefabs/enemies/sniper/sniper.tscn prefabs/enemies/flanker/flanker.tscn prefabs/enemies/swarmer/swarmer.tscn prefabs/enemies/suicider/suicider.tscn && grep -n "radius" prefabs/enemies/suicider/suicider-explosion.tscn</automated>
  </verify>
  <acceptance_criteria>
    - beeliner.tscn contains `max_health = 60` and `fight_range = 16000` and `bullet_speed = 6160`
    - sniper.tscn contains `max_health = 100` and `fight_range = 22000` and `bullet_speed = 14000`
    - flanker.tscn contains `max_health = 80` and `fight_range = 9000` and `bullet_speed = 8470`
    - swarmer.tscn contains `max_health = 30` and `fight_range = 10000` and `bullet_speed = 4900`
    - suicider.tscn contains `max_health = 40` and `max_speed = 5200` and `thrust = 2600`
    - suicider-explosion.tscn contains `radius = 1013.0` and `energy = 26250.0` and `kinetic = 7500.0`
    - No score_value lines were changed (beeliner=100, sniper=200, flanker=150, swarmer=50, suicider=75)
  </acceptance_criteria>
  <done>All 5 enemy scenes have doubled HP, doubled fight_range, 1.4x bullet_speed. Suicider has +30% speed/thrust, explosion has +50% radius/damage.</done>
</task>

<task type="auto">
  <name>Task 2: Rotate Polygon2D Shape nodes for vertex-forward orientation (per D-12)</name>
  <files>
    prefabs/enemies/sniper/sniper.tscn,
    prefabs/enemies/flanker/flanker.tscn,
    prefabs/enemies/swarmer/swarmer.tscn
  </files>
  <read_first>
    prefabs/enemies/sniper/sniper.tscn,
    prefabs/enemies/flanker/flanker.tscn,
    prefabs/enemies/swarmer/swarmer.tscn,
    prefabs/enemies/beeliner/beeliner.tscn
  </read_first>
  <action>
Add a `rotation` property to the `[node name="Shape" type="Polygon2D" ...]` block in each .tscn file that needs it. The `rotation` value is in radians. Place it as the first property after the `[node name="Shape" ...]` header line (before `color`).

**sniper.tscn** — The Shape node has a square polygon (`-200,-200, 200,-200, 200,200, -200,200`). Edges face +X, not a vertex. Add `rotation = 0.785398` (PI/4 = 45 degrees) to the `[node name="Shape" type="Polygon2D" parent="."]` block. This rotates the square so a corner points toward +X (the facing direction set by `look_at()`).

**flanker.tscn** — The Shape node has a pentagon with top vertex at (0, -250). Add `rotation = -1.5708` (-PI/2 = -90 degrees) to the `[node name="Shape" type="Polygon2D" parent="."]` block. This rotates the pentagon so the (0,-250) vertex points toward +X.

**swarmer.tscn** — The Shape node has a triangle with top vertex at (0, -250). Add `rotation = -1.5708` (-PI/2 = -90 degrees) to the `[node name="Shape" type="Polygon2D" parent="."]` block. This rotates the triangle so the (0,-250) vertex points toward +X.

**Beeliner** — Already correct (hexagon has vertex at (250, 0) on +X). No change needed.
**Suicider** — Circular (16-point approximation). No change needed (rotationally symmetric).

Example of what the Shape node block should look like after edit (sniper):
```
[node name="Shape" type="Polygon2D" parent="."]
rotation = 0.785398
color = Color(1, 0, 1, 1)
polygon = PackedVector2Array(...)
```
  </action>
  <verify>
    <automated>cd /Users/milan.kormout/Projects/personal/graviton && grep -A1 'name="Shape"' prefabs/enemies/sniper/sniper.tscn prefabs/enemies/flanker/flanker.tscn prefabs/enemies/swarmer/swarmer.tscn | grep "rotation"</automated>
  </verify>
  <acceptance_criteria>
    - sniper.tscn Shape node contains `rotation = 0.785398`
    - flanker.tscn Shape node contains `rotation = -1.5708`
    - swarmer.tscn Shape node contains `rotation = -1.5708`
    - beeliner.tscn Shape node does NOT have a new rotation line added
    - suicider.tscn Shape node does NOT have a new rotation line added
  </acceptance_criteria>
  <done>Sniper, Flanker, and Swarmer Polygon2D shapes are rotated so a vertex points in the +X facing direction. Beeliner and Suicider unchanged (already correct/circular).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Pure scene file value changes; no input, no network, no file I/O |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering | .tscn export values | accept | Local files only; no multiplayer or remote config. Values are developer-set constants. |
</threat_model>

<verification>
- Open each enemy scene in Godot editor and verify the exported values match the target table
- Run the game, spawn enemies, confirm they take more hits to destroy
- Visually confirm Sniper (diamond orientation), Flanker (pentagon vertex forward), Swarmer (triangle vertex forward)
</verification>

<success_criteria>
All 5 enemy types have doubled HP, doubled engagement range, and 1.4x bullet speed. Sniper/Flanker/Swarmer shapes point a vertex toward their facing direction. Suicider is 30% faster with 50% larger explosion.
</success_criteria>

<output>
After completion, create `.planning/phases/14-enemy-balancing-wave-variety-ui-polish/14-01-SUMMARY.md`
</output>
