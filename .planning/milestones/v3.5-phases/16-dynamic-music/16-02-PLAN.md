---
phase: 16-dynamic-music
plan: 02
type: execute
wave: 2
depends_on:
  - 16-01
files_modified:
  - world.gd
autonomous: false
requirements:
  - MUS-01
  - MUS-04

must_haves:
  truths:
    - "MusicManager is wired to WaveManager via world.gd"
    - "Music starts playing automatically when the game launches"
    - "Music category shifts when wave number crosses thresholds"
    - "Cross-fade sounds smooth between category transitions"
  artifacts:
    - path: "world.gd"
      provides: "MusicManager.connect_to_wave_manager wiring"
      contains: "MusicManager.connect_to_wave_manager"
  key_links:
    - from: "world.gd"
      to: "components/music-manager.gd"
      via: "MusicManager.connect_to_wave_manager($WaveManager)"
      pattern: "MusicManager.connect_to_wave_manager"
    - from: "components/music-manager.gd"
      to: "components/wave-manager.gd"
      via: "wave_started signal connection"
      pattern: "wm.wave_started.connect"
---

<objective>
Wire MusicManager to WaveManager in world.gd and verify the complete music system works end-to-end in-game.

Purpose: Completes the integration loop -- MusicManager receives wave_started signals and shifts music category automatically. The human checkpoint confirms music plays at launch, cross-fades sound smooth, and category transitions happen at the correct wave thresholds.

Output: Updated `world.gd` with MusicManager wiring
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-dynamic-music/16-CONTEXT.md
@.planning/phases/16-dynamic-music/16-01-SUMMARY.md

@world.gd
@components/music-manager.gd
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire MusicManager to WaveManager in world.gd</name>
  <files>world.gd</files>
  <read_first>
    - world.gd (current _ready() function, lines 54-80 -- see ScoreManager wiring at lines 65-67 for exact pattern)
    - components/music-manager.gd (verify connect_to_wave_manager function exists)
  </read_first>
  <action>
Add two lines to `world.gd` inside the `_ready()` function, immediately after the ScoreManager wiring block (after line 67, before line 68 `$WaveManager.wave_cleared_waiting.connect(...)`). Insert:

```gdscript
	# Wire MusicManager to WaveManager for wave-driven music (Phase 16)
	if MusicManager:
		MusicManager.connect_to_wave_manager($WaveManager)
```

This mirrors the exact pattern on lines 65-67 (ScoreManager wiring):
- Guard with `if MusicManager:` in case autoload is missing
- Call `connect_to_wave_manager($WaveManager)` passing the scene's WaveManager node

Do not modify any other part of world.gd. The indentation uses tabs (matching existing world.gd style).
  </action>
  <verify>
    <automated>grep "MusicManager.connect_to_wave_manager" world.gd</automated>
  </verify>
  <acceptance_criteria>
    - world.gd contains `if MusicManager:` guard check
    - world.gd contains `MusicManager.connect_to_wave_manager($WaveManager)` on the next line after the guard
    - The new lines appear after the ScoreManager wiring block (after `ScoreManager.connect_to_wave_manager($WaveManager)`)
    - The new lines appear before `$WaveManager.wave_cleared_waiting.connect`
    - Existing world.gd code is unchanged except for the insertion
  </acceptance_criteria>
  <done>world.gd wires MusicManager to WaveManager using the same pattern as ScoreManager</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify music system works end-to-end in-game</name>
  <what-built>The complete MusicManager autoload system: preload catalog with 4 MP3 tracks across 3 categories, dual AudioStreamPlayer cross-fade, wave-driven category selection, and world.gd wiring to WaveManager.</what-built>
  <how-to-verify>
    1. Open the Godot editor and run the project (F5)
    2. **MUS-01 check:** Confirm music starts playing automatically when the game launches -- you should hear an ambient track immediately
    3. Check the Godot Output panel for `[MusicManager] Started playback: ambient` and `[MusicManager] Connected to WaveManager wave_started signal`
    4. **MUS-03 + MUS-04 check:** Play through waves 1-5 -- music should remain ambient (no category change)
    5. When wave 6 starts, check the Output panel for `[MusicManager] Category changed to: combat (wave 6)` -- music should cross-fade to a combat track
    6. **MUS-05 check:** Listen to the cross-fade transition -- it should take about 2 seconds, with the old track fading out while the new track fades in simultaneously. It should sound smooth, not abrupt.
    7. Continue to wave 11 -- check for `[MusicManager] Category changed to: high_intensity (wave 11)` and another smooth cross-fade
    8. Type "approved" if all checks pass, or describe any issues
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| world.gd -> MusicManager | world.gd passes scene node reference to autoload |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-03 | T (Tampering) | world.gd wiring | accept | Guard `if MusicManager:` prevents null reference if autoload is removed. No user input involved in wiring. |
</threat_model>

<verification>
1. `grep "MusicManager.connect_to_wave_manager" world.gd` returns a match
2. Run the game in Godot editor -- music plays immediately
3. Godot Output shows `[MusicManager] Started playback: ambient`
4. Godot Output shows `[MusicManager] Connected to WaveManager wave_started signal`
5. At wave 6, Output shows category change to combat with audible cross-fade
6. At wave 11, Output shows category change to high_intensity with audible cross-fade
</verification>

<success_criteria>
- world.gd contains MusicManager.connect_to_wave_manager wiring
- Music plays automatically on game start (MUS-01)
- Category shifts at wave 6 (combat) and wave 11 (high_intensity) with smooth cross-fades (MUS-04, MUS-05)
- Human confirms audio sounds correct in-game
</success_criteria>

<output>
After completion, create `.planning/phases/16-dynamic-music/16-02-SUMMARY.md`
</output>
