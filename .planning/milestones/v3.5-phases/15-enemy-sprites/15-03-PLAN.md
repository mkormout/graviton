---
phase: 15-enemy-sprites
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - prefabs/enemies/beeliner/beeliner-bullet.tscn
  - prefabs/enemies/sniper/sniper-bullet.tscn
  - prefabs/enemies/flanker/flanker-bullet.tscn
  - prefabs/enemies/swarmer/swarmer-bullet.tscn
autonomous: true
requirements: [SPR-01, SPR-02]
tags: [godot, scene, sprite, bullet]

must_haves:
  truths:
    - "Each firing enemy's bullet scene displays a sprite from ships_assests.png instead of the default empty Sprite2D"
    - "Bullet Sprite2D nodes keep their existing rotation = 1.5708 (90°) to stay oriented with firing direction"
    - "Suicider has no bullet scene — confirmed by D-02 — so no suicider-bullet update exists"
    - "If the atlas file is missing at runtime the bullet still functions — Sprite2D.texture is null, bullet renders nothing visually but collision/damage still work"
  artifacts:
    - path: prefabs/enemies/beeliner/beeliner-bullet.tscn
      provides: "Beeliner bullet sprite (ENM-07 bullet region)"
      contains: "region_enabled"
    - path: prefabs/enemies/sniper/sniper-bullet.tscn
      provides: "Sniper bullet sprite (ENM-08 bullet region)"
      contains: "region_enabled"
    - path: prefabs/enemies/flanker/flanker-bullet.tscn
      provides: "Flanker bullet sprite (ENM-09 bullet region)"
      contains: "region_enabled"
    - path: prefabs/enemies/swarmer/swarmer-bullet.tscn
      provides: "Swarmer bullet sprite (ENM-10 bullet region)"
      contains: "region_enabled"
  key_links:
    - from: "bullet scenes Sprite2D"
      to: "res://ships_assests.png (ExtResource)"
      via: "ExtResource reference"
      pattern: "ExtResource.*ships_assests\\.png"
---

<objective>
Configure the existing empty Sprite2D node in each of the four firing-enemy bullet scenes (Beeliner, Sniper, Flanker, Swarmer) to display a sprite region from ships_assests.png. Suicider has no bullet scene per D-02. This is a pure scene-level change — no script edits, no new nodes.

Purpose: SPR-01 requires bullet sprites in addition to ship sprites (per D-01). SPR-02 requires hardcoded Rect2 regions. Bullet regions come from the atlas bottom half.
Output: Four modified bullet .tscn files. Bullets fired by all four enemy types now render with atlas sprites instead of being invisible (the Sprite2D was empty — only collision was visible).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/15-enemy-sprites/15-CONTEXT.md
@.planning/phases/15-enemy-sprites/15-RESEARCH.md
@.planning/phases/15-enemy-sprites/15-PATTERNS.md

# Target scenes
@prefabs/enemies/beeliner/beeliner-bullet.tscn
@prefabs/enemies/sniper/sniper-bullet.tscn
@prefabs/enemies/flanker/flanker-bullet.tscn
@prefabs/enemies/swarmer/swarmer-bullet.tscn

<interfaces>
<!-- Existing bullet scene structure (verified in beeliner-bullet.tscn and
sniper-bullet.tscn — they all follow the same pattern):

  [gd_scene load_steps=6 format=3 uid="..."]

  [ext_resource type="Script" path="res://components/enemy-bullet.gd" id="1_bullet"]
  [ext_resource type="PackedScene" ... id="2_explosion"]
  [ext_resource type="Script" path="res://components/damage.gd" id="3_damage"]

  [sub_resource type="Resource" id="Resource_damage"] ...
  [sub_resource type="RectangleShape2D" id="RectangleShape2D_shape"] ...

  [node name="{EnemyName}Bullet" type="RigidBody2D"] ... (script, attack, death, life)

  [node name="Sprite2D" type="Sprite2D" parent="."]
  rotation = 1.5708                 ← PRESERVE: bullet art orientation

  [node name="CollisionShape2D" type="CollisionShape2D" parent="."]
  rotation = 1.5708
  shape = SubResource(...)

The Sprite2D has rotation = 1.5708 (90°) already set — this is the offset that
rotates the "up-facing" atlas bullet art to align with the bullet's +X flight
direction. DO NOT remove it.

The new ExtResource id MUST be unique within each file. Convention used here:
  - id="4_atlas"  (one after the highest existing id)

Per-bullet region Rect2 values (from 15-PATTERNS.md table lines 220-228, all
ASSUMED — verified in Plan 04 editor task):

| Bullet scene             | region_rect                   |
|--------------------------|-------------------------------|
| beeliner-bullet.tscn     | Rect2(20, 1060, 120, 280)     |
| sniper-bullet.tscn       | Rect2(440, 1060, 120, 320)    |
| flanker-bullet.tscn      | Rect2(870, 1060, 120, 300)    |
| swarmer-bullet.tscn      | Rect2(1295, 1060, 100, 240)   |

Sprite2D scale = Vector2(0.3, 0.3) initial — bullet atlas art is much larger
than the RectangleShape2D collision. Plan 04 adjusts during playtest.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add atlas ExtResource and configure Sprite2D region in all four bullet scenes</name>

  <files>prefabs/enemies/beeliner/beeliner-bullet.tscn, prefabs/enemies/sniper/sniper-bullet.tscn, prefabs/enemies/flanker/flanker-bullet.tscn, prefabs/enemies/swarmer/swarmer-bullet.tscn</files>

  <read_first>
    - prefabs/enemies/beeliner/beeliner-bullet.tscn (full file — 33 lines)
    - prefabs/enemies/sniper/sniper-bullet.tscn (full file — 33 lines)
    - prefabs/enemies/flanker/flanker-bullet.tscn (full file — confirm Sprite2D has rotation = 1.5708)
    - prefabs/enemies/swarmer/swarmer-bullet.tscn (full file — confirm Sprite2D presence; note swarmer may use a different bullet visual)
    - .planning/phases/15-enemy-sprites/15-PATTERNS.md (bullet region table lines 220-228)
  </read_first>

  <action>
Apply the same modification pattern to all four bullet scenes. Each file needs:

1. `load_steps=` incremented by 1 in the `[gd_scene ...]` header (new ExtResource added).
2. A new `[ext_resource]` block referencing the atlas PNG, added after the last existing ext_resource.
3. The existing empty `[node name="Sprite2D" type="Sprite2D" parent="."]` block rewritten to include `texture`, `region_enabled`, `region_rect`, and `scale` properties. The existing `rotation = 1.5708` MUST be preserved.

STEP-BY-STEP for EACH file:

STEP 1 — Update the header.

Current (in beeliner-bullet.tscn): `[gd_scene load_steps=6 format=3 uid="uid://beeliner_bullet_001"]`
Change to: `[gd_scene load_steps=7 format=3 uid="uid://beeliner_bullet_001"]`

Apply the same `+1` increment to each of the four files.

STEP 2 — Add the atlas ExtResource.

Insert this line AFTER the last existing `[ext_resource ...]` line (before the first `[sub_resource ...]` block):

```
[ext_resource type="Texture2D" path="res://ships_assests.png" id="4_atlas"]
```

STEP 3 — Rewrite the Sprite2D node block.

Locate the existing block (in beeliner-bullet.tscn it is lines 27-28):
```
[node name="Sprite2D" type="Sprite2D" parent="."]
rotation = 1.5708
```

Replace with per-file version below. Preserve the surrounding blank lines.

----- beeliner-bullet.tscn -----
```
[node name="Sprite2D" type="Sprite2D" parent="."]
rotation = 1.5708
texture = ExtResource("4_atlas")
region_enabled = true
region_rect = Rect2(20, 1060, 120, 280)
scale = Vector2(0.3, 0.3)
```

----- sniper-bullet.tscn -----
```
[node name="Sprite2D" type="Sprite2D" parent="."]
rotation = 1.5708
texture = ExtResource("4_atlas")
region_enabled = true
region_rect = Rect2(440, 1060, 120, 320)
scale = Vector2(0.3, 0.3)
```

----- flanker-bullet.tscn -----
```
[node name="Sprite2D" type="Sprite2D" parent="."]
rotation = 1.5708
texture = ExtResource("4_atlas")
region_enabled = true
region_rect = Rect2(870, 1060, 120, 300)
scale = Vector2(0.3, 0.3)
```

----- swarmer-bullet.tscn -----
```
[node name="Sprite2D" type="Sprite2D" parent="."]
rotation = 1.5708
texture = ExtResource("4_atlas")
region_enabled = true
region_rect = Rect2(1295, 1060, 100, 240)
scale = Vector2(0.3, 0.3)
```

CRITICAL RULES:
- DO NOT modify the CollisionShape2D, script, attack, death, life, or any other node/resource in each file.
- DO NOT change existing ExtResource ids (keep id="1_bullet", "2_explosion", "3_damage" as-is). The new ExtResource id MUST be "4_atlas" (next free slot).
- DO NOT remove `rotation = 1.5708` from Sprite2D — this keeps atlas "up-facing" art aligned with bullet +X flight direction.
- If `swarmer-bullet.tscn` uses a higher existing ExtResource id (e.g., id="4_..." already taken), use id="5_atlas" or next free number — verify before edit.
- DO NOT add a region_enabled or texture property to any node OTHER than Sprite2D.
- Bullet script (`components/enemy-bullet.gd`) is NOT touched.
  </action>

  <verify>
    <automated>for f in prefabs/enemies/beeliner/beeliner-bullet.tscn prefabs/enemies/sniper/sniper-bullet.tscn prefabs/enemies/flanker/flanker-bullet.tscn prefabs/enemies/swarmer/swarmer-bullet.tscn; do grep -q "path=\"res://ships_assests.png\"" "$f" && grep -q "region_enabled = true" "$f" && grep -q "rotation = 1.5708" "$f" || { echo "FAIL $f"; exit 1; }; done; echo OK</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "path=\"res://ships_assests.png\"" prefabs/enemies/beeliner/beeliner-bullet.tscn` returns 1
    - `grep -c "path=\"res://ships_assests.png\"" prefabs/enemies/sniper/sniper-bullet.tscn` returns 1
    - `grep -c "path=\"res://ships_assests.png\"" prefabs/enemies/flanker/flanker-bullet.tscn` returns 1
    - `grep -c "path=\"res://ships_assests.png\"" prefabs/enemies/swarmer/swarmer-bullet.tscn` returns 1
    - `grep -n "region_rect = Rect2(20, 1060, 120, 280)" prefabs/enemies/beeliner/beeliner-bullet.tscn` returns 1 match
    - `grep -n "region_rect = Rect2(440, 1060, 120, 320)" prefabs/enemies/sniper/sniper-bullet.tscn` returns 1 match
    - `grep -n "region_rect = Rect2(870, 1060, 120, 300)" prefabs/enemies/flanker/flanker-bullet.tscn` returns 1 match
    - `grep -n "region_rect = Rect2(1295, 1060, 100, 240)" prefabs/enemies/swarmer/swarmer-bullet.tscn` returns 1 match
    - `grep -c "region_enabled = true" prefabs/enemies/beeliner/beeliner-bullet.tscn prefabs/enemies/sniper/sniper-bullet.tscn prefabs/enemies/flanker/flanker-bullet.tscn prefabs/enemies/swarmer/swarmer-bullet.tscn` returns 4 (one per file)
    - `grep -c "rotation = 1.5708" prefabs/enemies/beeliner/beeliner-bullet.tscn prefabs/enemies/sniper/sniper-bullet.tscn prefabs/enemies/flanker/flanker-bullet.tscn prefabs/enemies/swarmer/swarmer-bullet.tscn` returns 8 (2 per file — Sprite2D and CollisionShape2D)
    - `grep -c "load_steps=7" prefabs/enemies/beeliner/beeliner-bullet.tscn` returns 1 (incremented from 6)
    - `grep -c "load_steps=7\|load_steps=[7-9]" prefabs/enemies/sniper/sniper-bullet.tscn` returns 1 (incremented)
    - `grep -n "scale = Vector2(0.3, 0.3)" prefabs/enemies/beeliner/beeliner-bullet.tscn prefabs/enemies/sniper/sniper-bullet.tscn prefabs/enemies/flanker/flanker-bullet.tscn prefabs/enemies/swarmer/swarmer-bullet.tscn` returns 4 matches
    - `grep -c "id=\"1_bullet\"" prefabs/enemies/beeliner/beeliner-bullet.tscn` returns 1 (existing ExtResource preserved)
    - `ls prefabs/enemies/suicider/` returns no `suicider-bullet.tscn` file (D-02 confirmation — Suicider has no bullet scene)
  </acceptance_criteria>

  <done>
    All four bullet scenes reference res://ships_assests.png via a new ExtResource, their Sprite2D nodes have region_enabled = true with per-bullet region_rect from the Interfaces table, scale = Vector2(0.3, 0.3), and preserved rotation = 1.5708. All existing nodes and properties (CollisionShape2D, damage, explosion, life) untouched. No Suicider bullet scene exists (confirmed).
  </done>
</task>

</tasks>

<verification>
Bullet-scene completeness check:
- 4 bullet .tscn files reference the atlas via ExtResource
- 4 bullet .tscn files have region_enabled = true on Sprite2D
- 4 bullet .tscn files have scale = Vector2(0.3, 0.3) on Sprite2D
- 4 bullet .tscn files preserve rotation = 1.5708 on Sprite2D
- Suicider has no bullet scene (D-02)
- `components/enemy-bullet.gd` is unchanged

Visual correctness of bullet regions is deferred to Plan 04.
</verification>

<success_criteria>
- 4 files modified
- Each file gains exactly 1 new ExtResource (atlas) and 4 new properties on Sprite2D (texture, region_enabled, region_rect, scale)
- Each file's load_steps header incremented by 1
- All per-bullet region_rect values match the Interfaces table
- No existing node removed or reordered
</success_criteria>

<output>
After completion, create `.planning/phases/15-enemy-sprites/15-03-SUMMARY.md` recording:
- Per-bullet region_rect and scale actually committed
- Confirmation that all four bullet scenes load without parse errors
- Note any swarmer-bullet.tscn structural differences that required id adjustment
</output>
</content>
</invoke>