---
quick_id: 260419-0rt
slug: rpg-tuning-lock-bracket-homing
description: "RPG tuning: lock time 1s, bracket sizes 3x bigger, all-mount bracket visibility, rocket aims at target on fire"
date: 2026-04-19
---

# Quick Task 260419-0rt: RPG Tuning

## Tasks

### T1: Lock time 1s + bracket sizes 3x
**File:** `prefabs/rpg/rpg-weapon.gd` — LOCK_TIME 4.0 → 1.0
**File:** `prefabs/ui/weapon-hud.gd` — bracket lerp 120→30 becomes 360→90

### T2: Show lock brackets for all mounted RPGs
**File:** `prefabs/ui/weapon-hud.gd`
Iterate `_ship.get_mounts()` instead of only `_mount_front`. Maintain a `_lock_brackets: Dictionary` mapping weapon instance ID → Control (duplicated from the scene's `_lock_bracket` node). Create/remove entries dynamically each frame.

### T3: Aim rockets toward target on fire
**File:** `prefabs/rpg/rpg-weapon.gd`
In `fire()`: when locked, derive `fire_dir` from `(_lock_target.global_position - barrel.global_position).normalized()` and use it for both `instance.rotation` and `instance.linear_velocity`, instead of `global_rotation`.
