---
quick_id: 260427-144
status: Task 1 complete; Task 2 awaiting human playthrough
plan: 01
subsystem: waves
tags: [waves, world.gd, cosmic-theme, spawn-group, campaign-design]
requires:
  - "components/wave-manager.gd:SpawnGroup schema (260426-r2b, commit c26c778)"
provides:
  - "world.gd:$WaveManager.waves — 20 cosmic-themed waves (Pulsar -> Heat Death)"
affects:
  - world.gd
tech-stack:
  added: []
  patterns:
    - "Explicit groups[] form for every wave (no legacy enemy_scene shortcut)"
    - "Trailing commas on every dict + array entry (idiomatic GDScript 4)"
key-files:
  created: []
  modified:
    - world.gd
decisions:
  - "Dropped Performance test 2 (perf-test entry) entirely; replaced with themed wave 1 Pulsar."
  - "Cosmic naming theme; difficulty implied by the cosmic scale of the name."
  - "Escalating peaks with breathers at waves 4, 8, 9, 13, 14 (locked from CONTEXT)."
  - "Inline comments only on the three waves where the design choice is non-obvious (Cosmic Microwave, Event Horizon, Heat Death). All other waves are self-evident from the label."
metrics:
  duration: "~4 min"
  completed: 2026-04-26
---

# Quick Task 260427-144: Redesign all enemy waves in world.gd Summary

Replaced the entire `$WaveManager.waves` array in `world.gd` (lines 98-280) with a 20-wave cosmic-themed campaign that exercises every new SpawnGroup option introduced in quick task 260426-r2b. Difficulty follows the locked escalating-peaks-with-breathers curve.

## What Changed

- `world.gd` `$WaveManager.waves`: replaced 21 entries (1 perf-test + 20 placeholder graduating waves) with 20 themed cosmic waves.
- Wave order: Pulsar, Solar Wind, Comet Trail, Asteroid Belt, Nebula, Aurora Borealis, Magnetar, Solar Flare, Twin Suns, Neutron Star, Wormhole, Quasar, Dark Matter, Cosmic Microwave, Red Giant, Black Hole, Singularity, Event Horizon, Supernova, Heat Death.
- Every wave uses the explicit `"groups": [...]` form (no legacy single-group shortcut).
- Inline comments preserved only on Cosmic Microwave, Event Horizon, and Heat Death (per CLAUDE.md "no comments unless WHY non-obvious").

## Coverage Matrix (verified by grep)

| Option                              | Target | Actual count | Pass |
|-------------------------------------|-------:|-------------:|:----:|
| `spawn_at` occurrences              |   ~35  |          67  |  Y   |
| `stagger` occurrences               |    >=7 |           8  |  Y   |
| `cluster_radius` occurrences        |    >=5 |           6  |  Y   |
| `initial_state` occurrences         |    >=5 |           6  |  Y   |
| `position_mode: "random"` occurrences |    >=7 |           7  |  Y   |
| `position_mode: "exact"` occurrences  |   >=11 |          18  |  Y   |
| `angle_mode: "arc"` occurrences     |    >=9 |          14  |  Y   |
| `"label":` total                    |   = 20 |          20  |  Y   |
| `"groups":` total                   |   = 20 |          20  |  Y   |
| `"Performance test 2"` residue      |   = 0  |           0  |  Y   |

## Verification

- `godot --headless --check-only --quit world.gd` exits 0 (Godot 4.6.2.stable.official.71f334935). No SCRIPT ERROR.
- All grep counts above pass their targets.
- Commit: `ffa7b6f feat(waves): redesign campaign as 20 cosmic-themed waves exercising all SpawnGroup options`.
- Diff: `1 file changed, 155 insertions(+), 109 deletions(-)`.

## Deviations from Plan

None - plan executed exactly as written. The literal from Task 1's action block was applied verbatim with file-matching tab indentation.

## Pre-existing warnings (out of scope, not fixed)

The Godot parse run printed several `ext_resource, invalid UID` warnings on prefab `.tscn` files and an `ObjectDB instances leaked at exit` shutdown warning. These are unrelated to the wave array change and are pre-existing. Logged here for visibility; not deferred work for this task.

## Next Step

Task 2 is a human-verify checkpoint: in-game playthrough of the 20 waves to feel-check the difficulty curve, label order, and visual evidence of each new SpawnGroup option (arcs, clusters, drips, FIGHTING-state spawns). User runs `F5` in Godot, presses F to step waves.

## Self-Check: PASSED

- world.gd modified and committed (hash ffa7b6f) — verified via `git log --oneline -1`.
- Parse check passed (exit 0).
- All grep coverage targets met or exceeded.
- No accidental file deletions in the commit.
