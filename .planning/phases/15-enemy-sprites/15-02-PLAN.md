---
phase: 15-enemy-sprites
plan: 02
type: execute
wave: 2
depends_on: [15-01]
files_modified:
  - prefabs/enemies/beeliner/beeliner.tscn
  - prefabs/enemies/sniper/sniper.tscn
  - prefabs/enemies/flanker/flanker.tscn
  - prefabs/enemies/swarmer/swarmer.tscn
  - prefabs/enemies/suicider/suicider.tscn
autonomous: true
requirements: [SPR-01, SPR-02, SPR-04, SPR-05]
tags: [godot, scene, pointlight, visibility, gradient]

must_haves:
  truths:
    - "Each enemy .tscn has a PointLight2D child node named GemLight with per-enemy color matching D-04"
    - "Each enemy .tscn has a VisibleOnScreenNotifier2D child node with a rect sized to the enemy's visible area"
    - "Each enemy .tscn has a GradientTexture2D sub_resource used by the GemLight for radial falloff"
    - "Each GemLight has shadow_enabled = false (per RESEARCH pitfall 4 — wave-20 FPS)"
    - "The existing Sprite2D node in each enemy .tscn retains no region_rect/texture overrides — script sets them at runtime (SPR-03 fallback must remain)"
    - "The existing Shape Polygon2D node remains in each .tscn with visible=true default — script hides it on atlas load success"
  artifacts:
    - path: prefabs/enemies/beeliner/beeliner.tscn
      provides: "GemLight (green) + VisibleOnScreenNotifier2D + GradientTexture2D"
      contains: "GemLight"
    - path: prefabs/enemies/sniper/sniper.tscn
      provides: "GemLight (purple) + VisibleOnScreenNotifier2D + GradientTexture2D"
      contains: "GemLight"
    - path: prefabs/enemies/flanker/flanker.tscn
      provides: "GemLight (orange) + VisibleOnScreenNotifier2D + GradientTexture2D"
      contains: "GemLight"
    - path: prefabs/enemies/swarmer/swarmer.tscn
      provides: "GemLight (yellow/amber) + VisibleOnScreenNotifier2D + GradientTexture2D"
      contains: "GemLight"
    - path: prefabs/enemies/suicider/suicider.tscn
      provides: "GemLight (red) + VisibleOnScreenNotifier2D + GradientTexture2D"
      contains: "GemLight"
  key_links:
    - from: "enemy .tscn GemLight"
      to: "enemy .gd _setup_gem_light()"
      via: "node path $GemLight"
      pattern: "GemLight.*type=\"PointLight2D\""
    - from: "enemy .tscn VisibleOnScreenNotifier2D"
      to: "enemy .gd _setup_gem_light()"
      via: "node path $VisibleOnScreenNotifier2D"
      pattern: "VisibleOnScreenNotifier2D.*type=\"VisibleOnScreenNotifier2D\""
    - from: "GemLight texture"
      to: "GradientTexture2D sub_resource"
      via: "SubResource reference"
      pattern: "texture = SubResource"
---

<objective>
Add GemLight (PointLight2D) and VisibleOnScreenNotifier2D nodes to all five enemy ship scenes, plus the Gradient + GradientTexture2D sub_resources that the lights need for radial falloff. This is the scene-level complement to Plan 01: after this plan ships, the script `$GemLight` and `$VisibleOnScreenNotifier2D` references resolve at runtime and enemies start pulsing when on-screen.

Purpose: SPR-04 (pulsing gem glow with viewport culling) requires these scene nodes. SPR-01 and SPR-05 are partially satisfied here — the existing empty Sprite2D stays in place; runtime script configures region/scale. Gem colors and light rects are locked by D-04 and the scene values table.
Output: Five modified .tscn files; after this plan, enemies spawn on wave 20 and their gem lights pulse in viewport, disable off-screen, with no FPS regression (to be verified in Plan 04).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/15-enemy-sprites/15-CONTEXT.md
@.planning/phases/15-enemy-sprites/15-RESEARCH.md
@.planning/phases/15-enemy-sprites/15-PATTERNS.md

# Scene analog for PointLight2D + GradientTexture2D pattern
@prefabs/ship-bfg-23/ship-bfg-23.tscn

# Target scenes
@prefabs/enemies/beeliner/beeliner.tscn
@prefabs/enemies/sniper/sniper.tscn
@prefabs/enemies/flanker/flanker.tscn
@prefabs/enemies/swarmer/swarmer.tscn
@prefabs/enemies/suicider/suicider.tscn
@prefabs/enemies/base-enemy-ship.tscn

<interfaces>
<!-- Script side — @export vars added in Plan 01. These are populated by
runtime defaults in the enemy .gd files; the .tscn files do NOT need to set
them as node properties unless an override is desired. We leave them as script
defaults so Plan 04 editor tuning is one-click via Inspector. -->

Script node-path contract (from Plan 01's _setup_gem_light):
  $GemLight                  → PointLight2D child of RigidBody2D root
  $VisibleOnScreenNotifier2D → VisibleOnScreenNotifier2D child of RigidBody2D root
  $Sprite2D                  → already exists in base-enemy-ship.tscn (unchanged here)
  $Shape                     → Polygon2D (already exists — do NOT remove, remains fallback)

Per-enemy scene values (locked by CONTEXT D-04 and PATTERNS.md):

| Enemy     | GemLight color             | Notifier rect                      | Initial energy |
|-----------|----------------------------|------------------------------------|----------------|
| Beeliner  | Color(0.0, 0.9, 0.2, 1)    | Rect2(-400, -400, 800, 800)        | 0.5            |
| Sniper    | Color(0.6, 0.0, 1.0, 1)    | Rect2(-400, -400, 800, 800)        | 0.2            |
| Flanker   | Color(1.0, 0.45, 0.0, 1)   | Rect2(-300, -300, 600, 600)        | 0.4            |
| Swarmer   | Color(1.0, 0.75, 0.0, 1)   | Rect2(-200, -200, 400, 400)        | 0.3            |
| Suicider  | Color(1.0, 0.05, 0.05, 1)  | Rect2(-200, -200, 400, 400)        | 0.6            |

Initial energy matches gem_energy_min default from Plan 01 so the first pulse
frame has a consistent starting value.

Reference pattern from ship-bfg-23.tscn (PropellerMainEffectLight, lines 117-129):
  - PointLight2D with texture = SubResource("GradientTexture2D_*")
  - GradientTexture2D with fill = 1 (radial), fill_from = Vector2(0.5, 0.5), fill_to = Vector2(0.5, 1.0)
  - Gradient with colors = PackedColorArray(1,1,1,1, 1,1,1,0) (white center → transparent edge)
  - use_hdr = true on the GradientTexture2D
  - width = 256, height = 256

CRITICAL deviation from ship-bfg-23 reference: gem lights MUST use
shadow_enabled = false (player ship uses true for its single main light; gem
lights are many — pitfall 4 in RESEARCH).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add GemLight + VisibleOnScreenNotifier2D + GradientTexture2D to beeliner.tscn and suicider.tscn</name>

  <files>prefabs/enemies/beeliner/beeliner.tscn, prefabs/enemies/suicider/suicider.tscn</files>

  <read_first>
    - prefabs/enemies/beeliner/beeliner.tscn (current structure — 96 lines, load_steps=18)
    - prefabs/enemies/suicider/suicider.tscn (current structure — 84 lines, load_steps currently absent — regenerate if needed)
    - prefabs/ship-bfg-23/ship-bfg-23.tscn (GradientTexture2D + PointLight2D reference pattern, lines 22-32 and 117-129)
    - prefabs/enemies/base-enemy-ship.tscn (confirm existing Sprite2D node declaration at line 24)
    - .planning/phases/15-enemy-sprites/15-PATTERNS.md (scene values table lines 175-184)
  </read_first>

  <action>
Add nodes and sub_resources to each of the two enemy scenes. The existing node tree, script, @export overrides, CollisionShape2D, Shape (Polygon2D), DetectionArea, HitBox, Barrel, droppers, and timers MUST remain untouched. Insert new nodes at the end of the file so they become children of the root RigidBody2D.

----- prefabs/enemies/beeliner/beeliner.tscn -----

STEP 1 — Update `load_steps` in the `[gd_scene ...]` header. Current value is `load_steps=18`; increase by 2 (Gradient + GradientTexture2D sub_resources). New value: `load_steps=20`.

STEP 2 — Add two sub_resources. Place them AFTER the last `[sub_resource ...]` block (currently ends at the Resource_health_drop block) and BEFORE the first `[node ...]` block. Use these exact definitions:

```
[sub_resource type="Gradient" id="Gradient_gem_beeliner"]
colors = PackedColorArray(1, 1, 1, 1, 1, 1, 1, 0)

[sub_resource type="GradientTexture2D" id="GradientTexture2D_gem_beeliner"]
gradient = SubResource("Gradient_gem_beeliner")
width = 256
height = 256
use_hdr = true
fill = 1
fill_from = Vector2(0.5, 0.5)
fill_to = Vector2(0.5, 1.0)
```

STEP 3 — Append two new nodes at the END of the file (after the AmmoDropper node definition at the bottom). Use these exact definitions:

```
[node name="GemLight" type="PointLight2D" parent="."]
position = Vector2(0, 0)
color = Color(0.0, 0.9, 0.2, 1)
energy = 0.5
shadow_enabled = false
texture = SubResource("GradientTexture2D_gem_beeliner")
texture_scale = 8.0

[node name="VisibleOnScreenNotifier2D" type="VisibleOnScreenNotifier2D" parent="."]
rect = Rect2(-400, -400, 800, 800)
```

----- prefabs/enemies/suicider/suicider.tscn -----

STEP 1 — If the `[gd_scene ...]` header is missing `load_steps=`, add it with the calculated total. Count: current scene has 4 ext_resources + 2 Resource sub_resources (coin, health) + 4 CircleShape2D sub_resources = 10 load steps. After adding 2 new sub_resources = 12. Update/add: `load_steps=14` (10 existing + 2 sub_resources + 1 script + 1 death — but safest is to add `load_steps` without precise count since Godot re-numbers on save; use `load_steps=14` as a conservative upper bound — Godot will correct on next save).

If the header is `[gd_scene format=3 uid="uid://suicider_scene_001"]` with no load_steps, rewrite as `[gd_scene load_steps=14 format=3 uid="uid://suicider_scene_001"]`.

STEP 2 — Add two sub_resources. Place them BEFORE the first `[node ...]` block (before `[node name="Suicider" ...]`):

```
[sub_resource type="Gradient" id="Gradient_gem_suicider"]
colors = PackedColorArray(1, 1, 1, 1, 1, 1, 1, 0)

[sub_resource type="GradientTexture2D" id="GradientTexture2D_gem_suicider"]
gradient = SubResource("Gradient_gem_suicider")
width = 256
height = 256
use_hdr = true
fill = 1
fill_from = Vector2(0.5, 0.5)
fill_to = Vector2(0.5, 1.0)
```

STEP 3 — Append two new nodes at the END of the file (after the CoinDropper node definition):

```
[node name="GemLight" type="PointLight2D" parent="."]
position = Vector2(0, 0)
color = Color(1.0, 0.05, 0.05, 1)
energy = 0.6
shadow_enabled = false
texture = SubResource("GradientTexture2D_gem_suicider")
texture_scale = 10.0

[node name="VisibleOnScreenNotifier2D" type="VisibleOnScreenNotifier2D" parent="."]
rect = Rect2(-200, -200, 400, 400)
```

Note: Suicider uses texture_scale = 10.0 (slightly larger halo) because per CONTEXT specifics, the Suicider gem "IS most of the ship" and wants a dramatic glow.

CRITICAL RULES for both files:
- Do NOT delete or modify the existing `Shape` Polygon2D node — it is the SPR-03 fallback.
- Do NOT delete or modify the existing Sprite2D node inherited from base-enemy-ship.tscn (the script populates it at runtime).
- Do NOT add region_rect, texture, or scale to the Sprite2D — those are set by _setup_sprite() at runtime. Adding scene-level overrides would break SPR-03 fallback (the Polygon2D would stay hidden even when the atlas is missing).
- Do NOT change script references, @export overrides (max_health, thrust, detection_radius, etc.), collision layers, or any existing properties.
- GemLight `position = Vector2(0, 0)` is the ASSUMED starting gem position; Plan 04 will adjust after visual inspection in editor. Do NOT pre-offset the position.
- Parent all new nodes to `parent="."` (root RigidBody2D).
  </action>

  <verify>
    <automated>for f in prefabs/enemies/beeliner/beeliner.tscn prefabs/enemies/suicider/suicider.tscn; do grep -q "name=\"GemLight\"" "$f" && grep -q "name=\"VisibleOnScreenNotifier2D\"" "$f" && grep -q "type=\"GradientTexture2D\"" "$f" && grep -q "shadow_enabled = false" "$f" || { echo "FAIL $f"; exit 1; }; done; echo OK</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "name=\"GemLight\" type=\"PointLight2D\"" prefabs/enemies/beeliner/beeliner.tscn` returns 1
    - `grep -c "name=\"GemLight\" type=\"PointLight2D\"" prefabs/enemies/suicider/suicider.tscn` returns 1
    - `grep -c "name=\"VisibleOnScreenNotifier2D\" type=\"VisibleOnScreenNotifier2D\"" prefabs/enemies/beeliner/beeliner.tscn` returns 1
    - `grep -c "name=\"VisibleOnScreenNotifier2D\" type=\"VisibleOnScreenNotifier2D\"" prefabs/enemies/suicider/suicider.tscn` returns 1
    - `grep -n "color = Color(0.0, 0.9, 0.2, 1)" prefabs/enemies/beeliner/beeliner.tscn` returns 1 match (Beeliner green gem)
    - `grep -n "color = Color(1.0, 0.05, 0.05, 1)" prefabs/enemies/suicider/suicider.tscn` returns 1 match (Suicider red gem)
    - `grep -n "rect = Rect2(-400, -400, 800, 800)" prefabs/enemies/beeliner/beeliner.tscn` returns 1 match
    - `grep -n "rect = Rect2(-200, -200, 400, 400)" prefabs/enemies/suicider/suicider.tscn` returns 1 match
    - `grep -c "shadow_enabled = false" prefabs/enemies/beeliner/beeliner.tscn prefabs/enemies/suicider/suicider.tscn` returns 2 (one per file)
    - `grep -c "type=\"Gradient\" id=\"Gradient_gem_beeliner\"" prefabs/enemies/beeliner/beeliner.tscn` returns 1
    - `grep -c "type=\"Gradient\" id=\"Gradient_gem_suicider\"" prefabs/enemies/suicider/suicider.tscn` returns 1
    - `grep -c "type=\"GradientTexture2D\" id=\"GradientTexture2D_gem_beeliner\"" prefabs/enemies/beeliner/beeliner.tscn` returns 1
    - `grep -c "type=\"GradientTexture2D\" id=\"GradientTexture2D_gem_suicider\"" prefabs/enemies/suicider/suicider.tscn` returns 1
    - `grep -c "name=\"Shape\" type=\"Polygon2D\"" prefabs/enemies/beeliner/beeliner.tscn` returns 1 (existing fallback preserved)
    - `grep -c "name=\"Shape\" type=\"Polygon2D\"" prefabs/enemies/suicider/suicider.tscn` returns 1 (existing fallback preserved)
    - `grep -n "region_rect" prefabs/enemies/beeliner/beeliner.tscn` returns 0 matches (script handles at runtime — do NOT put region_rect in scene)
    - `grep -n "region_rect" prefabs/enemies/suicider/suicider.tscn` returns 0 matches
    - `grep -c "load_steps=" prefabs/enemies/beeliner/beeliner.tscn` returns 1 (header has load_steps)
    - `grep -c "load_steps=" prefabs/enemies/suicider/suicider.tscn` returns 1 (header has load_steps, added if missing)
    - `grep -n "texture_scale = 8.0" prefabs/enemies/beeliner/beeliner.tscn` returns 1 match
    - `grep -n "texture_scale = 10.0" prefabs/enemies/suicider/suicider.tscn` returns 1 match
    - `git diff prefabs/enemies/beeliner/beeliner.tscn | grep "^-" | grep -v "^---" | grep -v "load_steps" | wc -l` shows 0 (only load_steps line may be rewritten; all other lines additions)
  </acceptance_criteria>

  <done>
    Both beeliner.tscn and suicider.tscn have GemLight (with per-enemy color from D-04), VisibleOnScreenNotifier2D (with per-enemy rect from the Interfaces table), and a Gradient + GradientTexture2D sub_resource pair. All existing nodes (Shape Polygon2D, Sprite2D, collision, detection, droppers) are preserved. No region_rect set on Sprite2D (runtime script handles it per SPR-03). shadow_enabled = false on both gem lights.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add GemLight + VisibleOnScreenNotifier2D + GradientTexture2D to sniper.tscn, flanker.tscn, swarmer.tscn</name>

  <files>prefabs/enemies/sniper/sniper.tscn, prefabs/enemies/flanker/flanker.tscn, prefabs/enemies/swarmer/swarmer.tscn</files>

  <read_first>
    - prefabs/enemies/sniper/sniper.tscn (current structure — confirm load_steps and existing sub_resources)
    - prefabs/enemies/flanker/flanker.tscn (current structure)
    - prefabs/enemies/swarmer/swarmer.tscn (current structure)
    - prefabs/enemies/beeliner/beeliner.tscn (post-Task-1 state — reference for the exact sub_resource + node pattern)
    - .planning/phases/15-enemy-sprites/15-PATTERNS.md (scene values table lines 175-184)
  </read_first>

  <action>
Apply the SAME two-step pattern from Task 1 (add Gradient + GradientTexture2D sub_resources, then append GemLight + VisibleOnScreenNotifier2D nodes) to the three remaining enemy scenes. The structure is identical; only the color, rect, energy, and sub_resource ID differ per enemy.

For EACH file, perform:
1. Increment `load_steps=` in the `[gd_scene ...]` header by 2 (accounting for the two new sub_resources). If load_steps is absent, add it with a conservative value — Godot re-normalizes on save.
2. Add a `[sub_resource type="Gradient" id="Gradient_gem_{enemy}"]` block and a `[sub_resource type="GradientTexture2D" id="GradientTexture2D_gem_{enemy}"]` block immediately before the first `[node ...]` declaration. The Gradient colors PackedColorArray is always `(1, 1, 1, 1, 1, 1, 1, 0)` and the GradientTexture2D settings are always `width=256 height=256 use_hdr=true fill=1 fill_from=Vector2(0.5, 0.5) fill_to=Vector2(0.5, 1.0)` — identical across enemies.
3. Append two new nodes at the END of the file: GemLight (PointLight2D) and VisibleOnScreenNotifier2D, with per-enemy color/energy/rect.

Per-enemy values:

----- prefabs/enemies/sniper/sniper.tscn -----
Sub_resource suffix: `sniper`
Append at end:
```
[node name="GemLight" type="PointLight2D" parent="."]
position = Vector2(0, 0)
color = Color(0.6, 0.0, 1.0, 1)
energy = 0.2
shadow_enabled = false
texture = SubResource("GradientTexture2D_gem_sniper")
texture_scale = 8.0

[node name="VisibleOnScreenNotifier2D" type="VisibleOnScreenNotifier2D" parent="."]
rect = Rect2(-400, -400, 800, 800)
```

----- prefabs/enemies/flanker/flanker.tscn -----
Sub_resource suffix: `flanker`
Append at end:
```
[node name="GemLight" type="PointLight2D" parent="."]
position = Vector2(0, 0)
color = Color(1.0, 0.45, 0.0, 1)
energy = 0.4
shadow_enabled = false
texture = SubResource("GradientTexture2D_gem_flanker")
texture_scale = 7.0

[node name="VisibleOnScreenNotifier2D" type="VisibleOnScreenNotifier2D" parent="."]
rect = Rect2(-300, -300, 600, 600)
```

----- prefabs/enemies/swarmer/swarmer.tscn -----
Sub_resource suffix: `swarmer`
Append at end:
```
[node name="GemLight" type="PointLight2D" parent="."]
position = Vector2(0, 0)
color = Color(1.0, 0.75, 0.0, 1)
energy = 0.3
shadow_enabled = false
texture = SubResource("GradientTexture2D_gem_swarmer")
texture_scale = 5.0

[node name="VisibleOnScreenNotifier2D" type="VisibleOnScreenNotifier2D" parent="."]
rect = Rect2(-200, -200, 400, 400)
```

Rationale for texture_scale variation (per role tier D-08):
- Sniper/Beeliner (full size enemies): texture_scale = 8.0
- Flanker (75% size): texture_scale = 7.0
- Swarmer (50% size): texture_scale = 5.0
- Suicider (50% but dramatic gem): texture_scale = 10.0 (set in Task 1)

CRITICAL RULES (identical to Task 1):
- Do NOT delete or modify any existing nodes (Shape Polygon2D, Sprite2D, CollisionShape2D, DetectionArea, HitBox, Barrel, any existing droppers, timers, Flanker-specific Orbit nodes, Swarmer-specific nodes, etc.)
- Do NOT add region_rect, texture, or scale overrides to the Sprite2D node — runtime only.
- Parent all new nodes to `parent="."` (root RigidBody2D).
- Place sub_resources BEFORE the first `[node ...]` block in the file.
  </action>

  <verify>
    <automated>for f in prefabs/enemies/sniper/sniper.tscn prefabs/enemies/flanker/flanker.tscn prefabs/enemies/swarmer/swarmer.tscn; do grep -q "name=\"GemLight\"" "$f" && grep -q "name=\"VisibleOnScreenNotifier2D\"" "$f" && grep -q "type=\"GradientTexture2D\"" "$f" && grep -q "shadow_enabled = false" "$f" || { echo "FAIL $f"; exit 1; }; done; echo OK</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "name=\"GemLight\" type=\"PointLight2D\"" prefabs/enemies/sniper/sniper.tscn` returns 1
    - `grep -c "name=\"GemLight\" type=\"PointLight2D\"" prefabs/enemies/flanker/flanker.tscn` returns 1
    - `grep -c "name=\"GemLight\" type=\"PointLight2D\"" prefabs/enemies/swarmer/swarmer.tscn` returns 1
    - `grep -n "color = Color(0.6, 0, 1, 1)\|color = Color(0.6, 0.0, 1.0, 1)" prefabs/enemies/sniper/sniper.tscn` returns 1 match (Sniper purple)
    - `grep -n "color = Color(1, 0.45, 0, 1)\|color = Color(1.0, 0.45, 0.0, 1)" prefabs/enemies/flanker/flanker.tscn` returns 1 match (Flanker orange)
    - `grep -n "color = Color(1, 0.75, 0, 1)\|color = Color(1.0, 0.75, 0.0, 1)" prefabs/enemies/swarmer/swarmer.tscn` returns 1 match (Swarmer amber)
    - `grep -n "rect = Rect2(-400, -400, 800, 800)" prefabs/enemies/sniper/sniper.tscn` returns 1 match
    - `grep -n "rect = Rect2(-300, -300, 600, 600)" prefabs/enemies/flanker/flanker.tscn` returns 1 match
    - `grep -n "rect = Rect2(-200, -200, 400, 400)" prefabs/enemies/swarmer/swarmer.tscn` returns 1 match
    - `grep -c "shadow_enabled = false" prefabs/enemies/sniper/sniper.tscn prefabs/enemies/flanker/flanker.tscn prefabs/enemies/swarmer/swarmer.tscn` returns 3 (one per file)
    - `grep -c "id=\"Gradient_gem_sniper\"" prefabs/enemies/sniper/sniper.tscn` returns 1
    - `grep -c "id=\"Gradient_gem_flanker\"" prefabs/enemies/flanker/flanker.tscn` returns 1
    - `grep -c "id=\"Gradient_gem_swarmer\"" prefabs/enemies/swarmer/swarmer.tscn` returns 1
    - `grep -c "id=\"GradientTexture2D_gem_sniper\"" prefabs/enemies/sniper/sniper.tscn` returns 1
    - `grep -c "id=\"GradientTexture2D_gem_flanker\"" prefabs/enemies/flanker/flanker.tscn` returns 1
    - `grep -c "id=\"GradientTexture2D_gem_swarmer\"" prefabs/enemies/swarmer/swarmer.tscn` returns 1
    - `grep -c "name=\"Shape\" type=\"Polygon2D\"" prefabs/enemies/sniper/sniper.tscn` returns 1 (fallback preserved)
    - `grep -c "name=\"Shape\" type=\"Polygon2D\"" prefabs/enemies/flanker/flanker.tscn` returns 1
    - `grep -c "name=\"Shape\" type=\"Polygon2D\"" prefabs/enemies/swarmer/swarmer.tscn` returns 1
    - `grep -n "region_rect" prefabs/enemies/sniper/sniper.tscn prefabs/enemies/flanker/flanker.tscn prefabs/enemies/swarmer/swarmer.tscn` returns 0 matches (SPR-03 runtime path preserved)
    - `grep -n "texture_scale = 8.0" prefabs/enemies/sniper/sniper.tscn` returns 1 match
    - `grep -n "texture_scale = 7.0" prefabs/enemies/flanker/flanker.tscn` returns 1 match
    - `grep -n "texture_scale = 5.0" prefabs/enemies/swarmer/swarmer.tscn` returns 1 match
  </acceptance_criteria>

  <done>
    All three enemy scenes (sniper, flanker, swarmer) have a GemLight PointLight2D (with D-04 color), a VisibleOnScreenNotifier2D (with rect sized per D-08 role tier), a Gradient + GradientTexture2D sub_resource pair, and no changes to any existing node. shadow_enabled = false on all three gem lights.
  </done>
</task>

</tasks>

<verification>
Combined-plan scene-level check (runs after both tasks):
- All 5 enemy .tscn files contain a GemLight PointLight2D child of the root
- All 5 enemy .tscn files contain a VisibleOnScreenNotifier2D child of the root
- All 5 enemy .tscn files contain a Gradient + GradientTexture2D sub_resource pair
- All 5 enemy .tscn files have shadow_enabled = false on GemLight (pitfall 4)
- All 5 enemy .tscn files retain the existing Shape Polygon2D (SPR-03 fallback)
- No enemy .tscn has a region_rect or texture override on Sprite2D (SPR-03 runtime path)
- Gem colors match D-04 exactly: Beeliner green, Sniper purple, Flanker orange, Swarmer amber, Suicider red

Runtime behavior will be spot-checked in Plan 04 (Godot editor + playtest wave 20).
</verification>

<success_criteria>
- 5 .tscn files modified
- Each file adds exactly 2 new [node ...] blocks (GemLight, VisibleOnScreenNotifier2D) and 2 new [sub_resource ...] blocks (Gradient + GradientTexture2D)
- Per-enemy color, rect, energy, and texture_scale values match the Interfaces table exactly
- No existing nodes removed from any scene
- `load_steps=` header updated or added on each scene
</success_criteria>

<output>
After completion, create `.planning/phases/15-enemy-sprites/15-02-SUMMARY.md` recording:
- Per-enemy gem color, rect, and texture_scale actually committed (confirming Interfaces table match)
- Any Godot editor warnings on first load (if editor was opened)
- Confirmation that $GemLight and $VisibleOnScreenNotifier2D node paths resolve at runtime (i.e., Plan 01 + Plan 02 together produce a running game without script errors)
</output>
</content>
</invoke>