---
phase: 02-code-quality
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/mountable-body.gd
  - components/inventory-slot.gd
autonomous: true
requirements:
  - QUA-02
  - QUA-03

must_haves:
  truths:
    - "get_mount() iterates the pre-populated mounts array instead of calling find_children() — no scene-tree walk occurs on the per-frame path"
    - "No print() call fires when a player drags an inventory slot item"
  artifacts:
    - path: "components/mountable-body.gd"
      provides: "get_mount() cache fix"
      contains: "for mount in mounts:"
    - path: "components/inventory-slot.gd"
      provides: "print removal"
      not_contains: 'print("_get_drag_data'
  key_links:
    - from: "components/mountable-body.gd"
      to: "mounts array"
      via: "get_mount() iterates mounts"
      pattern: "for mount in mounts:"
---

<objective>
Fix two mechanical one-line issues: the per-frame `find_children()` hot path (QUA-02) and the drag-and-drop debug print (QUA-03).

Purpose: Eliminate wasted CPU from scene-tree walks in _physics_process and remove the noisy print statement from the drag hot path.
Output: Two modified files, both changes verifiable by code inspection.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-code-quality/02-CONTEXT.md
@.planning/phases/02-code-quality/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix get_mount() to iterate mounts cache (QUA-02)</name>
  <files>components/mountable-body.gd</files>
  <read_first>
    - components/mountable-body.gd (read full file — understand get_mount(), get_mounts(), and the mounts array population in mount_weapon())
  </read_first>
  <action>
In `components/mountable-body.gd`, change `get_mount()` (lines 48-53) to iterate the pre-populated `mounts` array instead of calling `get_mounts()`.

Current implementation:
```gdscript
func get_mount(tag: String = "") -> MountPoint:
    for mount in get_mounts():
        if mount.tag == tag:
            return mount
    
    return null
```

Change `get_mounts()` to `mounts` in the for loop:
```gdscript
func get_mount(tag: String = "") -> MountPoint:
    for mount in mounts:
        if mount.tag == tag:
            return mount
    
    return null
```

Do NOT change `get_mounts()` itself — it is only called from `mount_weapon()` (line 25) which runs on mount events, not per-frame. That function is not a hot path.

Do NOT change any other code in this file. One word changes: `get_mounts()` → `mounts` in the `get_mount()` body only.
  </action>
  <verify>grep -n "for mount in mounts:" /Users/milan.kormout/Projects/personal/graviton/components/mountable-body.gd</verify>
  <acceptance_criteria>
    - `grep "for mount in mounts:" components/mountable-body.gd` prints at least one result
    - `grep "for mount in get_mounts():" components/mountable-body.gd` prints no results (the hot-path call is gone)
    - `grep "mounts = get_mounts()" components/mountable-body.gd` still prints one result at line 25 (mount_weapon still populates the cache — do not remove this)
  </acceptance_criteria>
  <done>get_mount() iterates the mounts cache; find_children() is no longer called on the per-frame path</done>
</task>

<task type="auto">
  <name>Task 2: Remove debug print from inventory-slot drag hot path (QUA-03)</name>
  <files>components/inventory-slot.gd</files>
  <read_first>
    - components/inventory-slot.gd (read full file — confirm print location at line 71 before deleting)
  </read_first>
  <action>
In `components/inventory-slot.gd`, delete line 71:
```gdscript
    print("_get_drag_data: ", data)
```

The function `_get_drag_data` (lines 57-73) should look like this after the change:
```gdscript
func _get_drag_data(_at_position: Vector2) -> Variant:
    var data = null
    
    if occupant:
        data = {
            "source": self,
            "item_type": occupant,
            "quantity": quantity
        }
        
        set_drag_preview(
            make_drag_preview()
        )
    
    return data
```

No replacement is needed. This was a development debug statement. Delete the line and nothing else.
  </action>
  <verify>grep -n "print" /Users/milan.kormout/Projects/personal/graviton/components/inventory-slot.gd</verify>
  <acceptance_criteria>
    - `grep "print" components/inventory-slot.gd` prints no results
    - `grep "_get_drag_data" components/inventory-slot.gd` still prints the function definition (function itself is intact)
    - `grep "return data" components/inventory-slot.gd` prints one result (function still returns data correctly)
  </acceptance_criteria>
  <done>No print() calls remain in inventory-slot.gd; _get_drag_data still returns data correctly</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This phase contains no trust boundaries. Changes are internal GDScript edits with no network, input validation, authentication, session, or user-data concerns.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | N/A | All | accept | No applicable STRIDE threats — this is a pure internal GDScript refactor with no external inputs, no auth, no PII, no network calls. The Godot 4.2.1 game has no server component. |
</threat_model>

<verification>
After both tasks complete:

1. `grep "for mount in mounts:" components/mountable-body.gd` — must print one result
2. `grep "for mount in get_mounts():" components/mountable-body.gd` — must print no results
3. `grep "mounts = get_mounts()" components/mountable-body.gd` — must print one result (line 25 in mount_weapon)
4. `grep "print" components/inventory-slot.gd` — must print no results
</verification>

<success_criteria>
- get_mount() uses `for mount in mounts:` — no scene-tree walk per physics frame
- inventory-slot.gd contains no print() calls
- Both files are otherwise unchanged from their pre-plan state
</success_criteria>

<output>
After completion, create `.planning/phases/02-code-quality/02-01-SUMMARY.md`
</output>
